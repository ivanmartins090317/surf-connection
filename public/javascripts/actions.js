function actions() {
  const coment = document.querySelector(".coment");
  coment.classList.remove("is-hidden");
  if ((event = true)) {
    coment.classList.remove("is-hidden");
  }
}

function hiddenItem() {
  const buttonPost = document.querySelector(".post");
  buttonPost.classList.add("is-hidden");
}
